# Frontend API origin. For local development, point at the Spring backend.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Must match the backend CSRF cookie name.
NEXT_PUBLIC_CSRF_COOKIE_NAME=im_csrf_token
